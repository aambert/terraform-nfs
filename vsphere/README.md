# Terraform NFS with vSphere provider
Terraform NFS

You will need to update for your environment.
```
cp example-tfvars.txt terraform.tfvars
vim terraform.tfvars
cp example-variables.txt variables.tf
vim variables.tf
```

Commands:
```
terraform init
terraform plan
terraform apply
```
